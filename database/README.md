# Database

Codd owns this folder.

Current status:

- `current-schema.sql` is the existing six-table Supabase baseline.
- `migration-002-multi-supplier.sql` is an unapplied Phase 2A draft.
- `seed-current-schema.sql` seeds the current baseline.
- `schema.sql` is a legacy wider schema retained for comparison.
- `legacy-backend-setup.md` contains earlier setup instructions.

No migration in this folder is applied automatically. Production changes require explicit user approval.

